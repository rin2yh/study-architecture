profile: cover.out

threshold:
  total: 60

# api/db/di/client はいずれも生成コード (// Code generated)。go-test-coverage は生成物を
# 自動判定できないため path で除外する。internal/test はテストヘルパ。
exclude:
  paths:
    - server/[^/]+/api/
    - /internal/db/
    - /internal/di/
    - /internal/client/
    - /internal/test/
