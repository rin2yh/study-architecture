apiVersion: 1

# 単一情報源はリポジトリの JSON。手動作成・手動編集に依存させない。
providers:
  - name: ec-dashboards
    orgId: 1
    folder: EC Service Dashboards
    type: file
    disableDeletion: true
    allowUiUpdates: false
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
